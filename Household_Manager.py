import streamlit as st
from style import page_header

st.set_page_config(page_title="Household Manager", page_icon="🏠", layout="wide")

page_header("Dashboard", "Today's events, list items due, grocery count, unread notes")
st.write("Placeholder — real summary coming in Stage 14.")
